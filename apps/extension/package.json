{
  "name": "@fastcup/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "typecheck": "wxt prepare && tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "@fastcup/shared": "workspace:*",
    "p-limit": "^7.0.0",
    "preact": "^10.27.0",
    "seedrandom": "^3.0.5",
    "xlsx": "^0.18.5",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.0",
    "@types/seedrandom": "^3.0.8",
    "@vitest/coverage-v8": "^3.0.0",
    "@playwright/test": "^1.54.0",
    "vitest": "^3.0.0",
    "wxt": "^0.21.4"
  }
}
