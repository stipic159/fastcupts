{
  "name": "@fastcup/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint ."
  },
  "dependencies": {
    "@fastcup/shared": "workspace:*",
    "@fastify/cors": "^11.0.0",
    "@fastify/rate-limit": "^10.0.0",
    "fastify": "^5.0.0",
    "lru-cache": "^11.0.0",
    "p-limit": "^7.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": { "tsx": "^4.20.0", "vitest": "^3.0.0" }
}
