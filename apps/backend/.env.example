# Never expose this key to the extension, Git, or frontend build variables.
FACEIT_API_KEY=
HOST=127.0.0.1
PORT=3000
# Set the exact ID after loading the extension once in chrome://extensions.
CORS_ORIGIN=chrome-extension://YOUR_EXTENSION_ID
